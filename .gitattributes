# Git LFS configuration for large video files
# Files over 100MB tracked with LFS

*.mov filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text

# Track all videos in assets/videos folder with LFS
assets/videos/* filter=lfs diff=lfs merge=lfs -text
